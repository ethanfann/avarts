{
  "name": "avarts",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "codegen": "rake graphql:schema:dump && cd frontend && npm run codegen"
  },
  "devDependencies": {
    "@prettier/plugin-ruby": "^2.0.0",
    "prettier": "^2.4.1"
  }
}
