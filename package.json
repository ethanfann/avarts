{
  "name": "avarts",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "codegen": "rake graphql:schema:dump && cd frontend && npm run codegen",
    "start": "docker compose up && docker exec -it avarts-app-1 sh -c \"rails db:create && rails db:migrate && rails db:seed\""
  },
  "devDependencies": {
    "@prettier/plugin-ruby": "^2.0.0",
    "prettier": "^2.4.1"
  }
}
